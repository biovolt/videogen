---
phase: 03-pr-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ct/openMontage.sh
  - install/openMontage-install.sh
autonomous: true
requirements:
  - PR-01
  - PR-02
  - PR-03
  - PR-04

must_haves:
  truths:
    - "ShellCheck reports zero warnings on both scripts"
    - "Every install command uses the $STD prefix"
    - "All install steps are wrapped with msg_info/msg_ok messaging"
    - "Install script closes with motd_ssh, customize, cleanup_lxc in correct order"
    - "Copyright header appears before source line in ct/ script"
    - "update_script exits non-zero on error conditions"
    - "No bare exit (without numeric argument) remains in ct/ script"
    - "Version tracking is consistent between install and update"
  artifacts:
    - path: "ct/openMontage.sh"
      provides: "PR-ready host orchestrator"
      contains: "shellcheck disable=SC1090"
    - path: "install/openMontage-install.sh"
      provides: "PR-ready install script"
      contains: ".venv/bin/python3"
  key_links:
    - from: "install/openMontage-install.sh"
      to: "ct/openMontage.sh"
      via: "version tracking format"
      pattern: "OpenMontage_version.txt"
---

<objective>
Fix all remaining code review warnings and ensure both scripts pass every item on the community-scripts PR submission checklist.

Purpose: Both scripts already work. This plan addresses the 5 remaining review warnings (WR-01 through WR-04 from Phase 2, IN-01 from Phase 2) and the single ShellCheck warning (SC1090), then verifies all PR checklist items pass.

Output: Two clean, PR-ready shell scripts with zero ShellCheck warnings and full community-scripts compliance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-host-orchestrator-and-update/02-REVIEW.md
@.planning/phases/01-install-script/01-REVIEW.md
@ct/openMontage.sh
@install/openMontage-install.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ct/openMontage.sh review warnings and ShellCheck</name>
  <files>ct/openMontage.sh</files>
  <read_first>
    - ct/openMontage.sh
    - .planning/phases/02-host-orchestrator-and-update/02-REVIEW.md
  </read_first>
  <action>
Apply these six targeted fixes to ct/openMontage.sh:

1. **IN-01 -- Move copyright header before source line.** Reorder so lines 2-6 (copyright block) appear immediately after the shebang, BEFORE the `source <(curl ...)` line. Result:
   ```
   #!/usr/bin/env bash
   # Copyright (c) 2021-2026 community-scripts ORG
   # Author: calesthio
   # License: MIT | https://github.com/community-scripts/ProxmoxVE/raw/main/LICENSE
   # Source: https://github.com/calesthio/OpenMontage
   source <(curl -fsSL ...)
   ```

2. **SC1090 -- Add ShellCheck directive.** Add `# shellcheck disable=SC1090` on the line immediately before the `source <(curl ...)` line. This is the standard community-scripts pattern since ShellCheck cannot follow dynamic sources.

3. **WR-01a -- Exit with non-zero on error (line 30).** Change `exit` (no argument) on the "No installation found" error path to `exit 1` so the error propagates a failure code.

4. **WR-01b -- Exit with explicit code at end of update_script (line 60).** Change the bare `exit` at the end of update_script (in the else-branch, after "No update required" message) to `exit 0`. This is the success path so exit 0 is correct, and it eliminates the last bare exit.

5. **WR-03 -- Use absolute path for requirements.txt.** Change `-r requirements.txt` to `-r /opt/openmontage/requirements.txt` so the path is not fragile to working directory changes.

6. **WR-02 -- Checkout release tag after git pull.** After the `$STD git pull` line in update_script, add:
   ```bash
   $STD git -C /opt/openmontage checkout "${RELEASE}"
   ```
   This ensures the working tree matches the tracked release tag, not just the latest commit on the default branch.

Do NOT change anything else. Minimal edits only.
  </action>
  <verify>
    <automated>shellcheck /Users/johan/dev/Cn/video/ct/openMontage.sh 2>&1 | grep -c "warning\|error" | grep -q "^0$" && echo "PASS: ShellCheck clean" || echo "FAIL: ShellCheck warnings remain"; grep -n 'exit$' /Users/johan/dev/Cn/video/ct/openMontage.sh | grep -q . && echo "FAIL: bare exit still present" || echo "PASS: no bare exit"; grep -c 'exit 1' /Users/johan/dev/Cn/video/ct/openMontage.sh | grep -q '[1-9]' && echo "PASS: exit 1 present" || echo "FAIL: no exit 1 found"</automated>
  </verify>
  <acceptance_criteria>
    - ShellCheck reports zero warnings/errors on ct/openMontage.sh
    - Copyright header appears before source line (lines 2-5)
    - `exit 1` on the no-installation-found error path (formerly line 30)
    - `exit 0` at the end of update_script else-branch (formerly line 60)
    - No bare `exit` (without numeric argument) remains anywhere in the file
    - requirements.txt path is absolute
    - `git checkout "${RELEASE}"` after `git pull` in update_script
  </acceptance_criteria>
  <done>ct/openMontage.sh passes ShellCheck with zero warnings, has no bare exits, and all six review findings are resolved</done>
</task>

<task type="auto">
  <name>Task 2: Fix install script review warnings and verify PR checklist</name>
  <files>install/openMontage-install.sh</files>
  <read_first>
    - install/openMontage-install.sh
    - .planning/phases/02-host-orchestrator-and-update/02-REVIEW.md
    - .planning/phases/01-install-script/01-REVIEW.md
  </read_first>
  <action>
Apply these two targeted fixes to install/openMontage-install.sh:

1. **WR-04 -- Use venv Python instead of system python3.** On line 62, change `python3 -` to `/opt/openmontage/.venv/bin/python3 -`. The venv is created on line 44 (before this block), so it exists. This ensures the install script uses the same Python interpreter it installed.

2. **WR-02 -- Checkout release tag after clone.** After the RELEASE detection block (after line 38 where RELEASE is set via the short-SHA fallback, before writing to version.txt on line 39), add:
   ```bash
   if [[ -n "${RELEASE}" ]]; then
     $STD git -C /opt/openmontage checkout "${RELEASE}"
   fi
   ```
   This pins the working tree to the release tag when one exists. When RELEASE is the short SHA (no releases exist), checking out the current HEAD's short SHA is a no-op. The `-n` check simply guards against empty RELEASE which should not happen but is defensive.

Then verify the FULL PR checklist from CLAUDE.md against both files:
- Shebang is `#!/usr/bin/env bash` -- check both files
- build.func sourced from official URL -- check ct/ script
- Copyright header present -- check both files
- Author line present -- check both files
- License line present -- check both files
- Source line present -- check both files
- All `var_*` use `${var_name:-default}` form -- check ct/ script
- All commands prefixed with `$STD` -- check install script
- `motd_ssh`, `customize`, `cleanup_lxc` at end -- check install script
- Version saved to `/opt/${APP}_version.txt` -- check install script
- `update_script()` implemented -- check ct/ script

Do NOT change anything beyond the two fixes above. Minimal edits only.
  </action>
  <verify>
    <automated>shellcheck /Users/johan/dev/Cn/video/install/openMontage-install.sh 2>&1 | grep -c "warning\|error" | grep -q "^0$" && echo "PASS: ShellCheck clean" || echo "FAIL"; shellcheck /Users/johan/dev/Cn/video/ct/openMontage.sh 2>&1 | grep -c "warning\|error" | grep -q "^0$" && echo "PASS: ct/ clean" || echo "FAIL"; grep -q "motd_ssh" /Users/johan/dev/Cn/video/install/openMontage-install.sh && echo "PASS: motd_ssh present" || echo "FAIL"; grep -q "cleanup_lxc" /Users/johan/dev/Cn/video/install/openMontage-install.sh && echo "PASS: cleanup_lxc present" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - install/openMontage-install.sh uses `/opt/openmontage/.venv/bin/python3` instead of `python3` for the .env config script
    - Install script checks out the release tag after clone
    - Both scripts pass ShellCheck with zero warnings
    - All PR checklist items verified as passing
  </acceptance_criteria>
  <done>Both scripts are PR-ready: ShellCheck clean, all review warnings resolved, full PR checklist compliance verified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub API -> scripts | Release tag fetched from external API used in git checkout |
| .env.example -> .env | User config file created from repo template |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | git checkout "${RELEASE}" | mitigate | RELEASE value comes from GitHub API response parsed with grep/sed -- already validated as non-empty before use; git checkout of a tag is safe |
| T-03-02 | Information Disclosure | .env file | accept | .env is inside LXC container with root-only access; no network exposure. API keys commented out by default |
</threat_model>

<verification>
Run ShellCheck on both scripts and confirm zero warnings:
```bash
shellcheck ct/openMontage.sh install/openMontage-install.sh
```

Verify PR checklist items by inspection:
- Copyright headers before source lines
- All var_* use default form
- $STD prefix on all install commands
- motd_ssh + customize + cleanup_lxc closing sequence
- update_script implemented with proper error handling
- No bare `exit` without explicit exit code
</verification>

<success_criteria>
1. `shellcheck ct/openMontage.sh` returns zero warnings
2. `shellcheck install/openMontage-install.sh` returns zero warnings
3. All 5 review warnings (WR-01 through WR-04, IN-01) are resolved
4. No bare `exit` (without numeric argument) in ct/openMontage.sh
5. Every PR submission checklist item from CLAUDE.md passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-pr-polish/03-01-SUMMARY.md`
</output>
