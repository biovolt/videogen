---
phase: 02-host-orchestrator-and-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ct/openMontage.sh
  - install/openMontage-install.sh
autonomous: true
requirements:
  - CT-01
  - CT-02
  - CT-03
  - CT-04
  - UPD-01
  - UPD-02
  - UPD-03
  - UPD-04

must_haves:
  truths:
    - "ct/openMontage.sh follows the canonical community-scripts pattern (source build.func, var_* defaults, header_info/variables/color/catch_errors, update_script, start/build_container/description)"
    - "No local install_script() override exists — build.func's install_script() handles container creation flow"
    - "Default resources are 2 CPU, 2048 MB RAM, 12 GB disk"
    - "GPU passthrough dialog is available in advanced mode (var_gpu set)"
    - "update_script() detects version via GitHub Releases API, pulls code, reinstalls deps, preserves .env"
    - "All commands in update_script use $STD prefix"
    - "No pct exec or API key injection from host to container"
  artifacts:
    - path: "ct/openMontage.sh"
      provides: "Host orchestrator with canonical structure and update mechanism"
      contains: "var_disk.*12"
    - path: "install/openMontage-install.sh"
      provides: "In-container installer (cleaned up dead code)"
      contains: "motd_ssh"
  key_links:
    - from: "ct/openMontage.sh"
      to: "build.func"
      via: "source <(curl -fsSL ...)"
      pattern: "source.*build\\.func"
    - from: "ct/openMontage.sh"
      to: "install/openMontage-install.sh"
      via: "build_container() in build.func runs install script inside LXC"
      pattern: "build_container"
    - from: "ct/openMontage.sh update_script()"
      to: "/opt/OpenMontage_version.txt"
      via: "version comparison"
      pattern: "OpenMontage_version\\.txt"
---

<objective>
Rewrite ct/openMontage.sh to follow the canonical community-scripts pattern, removing the broken install_script() override and pct exec API key injection, fixing container defaults, adding GPU passthrough support, and completing the update_script() with proper msg_info/msg_ok wrapping. Also clean up the install script to remove dead references to the removed host-to-container key injection.

Purpose: The ct/ script is the entry point for Proxmox users. It must correctly delegate to build.func for container creation and provide a working update mechanism.
Output: A PR-ready ct/openMontage.sh and a cleaned-up install/openMontage-install.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-host-orchestrator-and-update/02-RESEARCH.md

<interfaces>
<!-- The ct/ script has no code imports — it sources build.func at runtime.
     Key framework functions used (from build.func): -->

From build.func (sourced at runtime on Proxmox host):
  header_info "$APP"    — displays app name banner
  variables             — registers var_* defaults for whiptail
  color                 — sets color variables (GN, YW, CL, etc.)
  catch_errors          — sets error trap (replaces set -e)
  start                 — routes: host -> install_script(), container -> update menu
  build_container       — creates LXC, starts it, runs install script inside via lxc-attach
  description           — writes container description in Proxmox UI
  check_container_storage   — verifies disk space (for update_script)
  check_container_resources — verifies CPU/RAM (for update_script)
  msg_info "text"       — blue spinner prefix
  msg_ok "text"         — green checkmark prefix
  msg_error "text"      — red X prefix

From install/openMontage-install.sh (runs inside container):
  Sources $FUNCTIONS_FILE_PATH (tools.func passed by build.func)
  Writes version to /opt/OpenMontage_version.txt
  Creates .env from .env.example with commented placeholders for API keys
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ct/openMontage.sh to canonical pattern</name>
  <files>ct/openMontage.sh</files>
  <read_first>
    - ct/openMontage.sh (current broken state — 77 lines)
    - .planning/phases/02-host-orchestrator-and-update/02-RESEARCH.md (canonical pattern in "Code Examples" section)
    - CLAUDE.md (var_* format rules, anti-patterns table)
  </read_first>
  <action>
Rewrite ct/openMontage.sh to match the canonical community-scripts ct/ pattern. The complete target script structure (verified from jellyfin.sh, node-red.sh, gitea.sh in RESEARCH.md):

1. Line 1: `source <(curl -fsSL https://raw.githubusercontent.com/community-scripts/ProxmoxVE/main/misc/build.func)` — note `-fsSL` not just `-s` (current script has `-s` only)

2. Copyright header block (keep existing, it is correct)

3. APP and var_* block — changes from current:
   - Keep: `APP="OpenMontage"`, var_tags, var_cpu (2), var_ram (2048), var_os (debian), var_version (12), var_unprivileged (1)
   - FIX: `var_disk="${var_disk:-12}"` (was 8, per D-01/CT-01)
   - ADD: `var_gpu="${var_gpu:-no}"` (per D-02/CT-03 — enables GPU dialog in advanced mode)

4. Four mandatory setup calls (keep existing, correct order):
   ```
   header_info "$APP"
   variables
   color
   catch_errors
   ```

5. update_script() function — rewrite with proper msg_info/msg_ok wrapping:
   ```bash
   function update_script() {
     header_info
     check_container_storage
     check_container_resources

     if [[ ! -f /opt/OpenMontage_version.txt ]]; then
       msg_error "No ${APP} installation found!"
       exit
     fi

     RELEASE=$(curl -fsSL https://api.github.com/repos/calesthio/OpenMontage/releases/latest | grep '"tag_name":' | sed -E 's/.*"([^"]+)".*/\1/')

     if [[ -z "${RELEASE}" ]]; then
       msg_error "Could not fetch latest release from GitHub"
       exit 1
     fi

     if [[ "${RELEASE}" != "$(cat /opt/OpenMontage_version.txt)" ]]; then
       msg_info "Updating ${APP} to ${RELEASE}"
       cd /opt/openmontage || { msg_error "Cannot find /opt/openmontage"; exit 1; }
       $STD git pull
       msg_ok "Pulled ${APP} ${RELEASE}"

       msg_info "Reinstalling Python dependencies"
       $STD uv pip install --python /opt/openmontage/.venv/bin/python -r requirements.txt
       msg_ok "Reinstalled Python dependencies"

       msg_info "Reinstalling Node.js dependencies"
       cd /opt/openmontage/remotion-composer || { msg_error "Cannot find remotion-composer"; exit 1; }
       $STD npm install
       msg_ok "Reinstalled Node.js dependencies"

       echo "${RELEASE}" >/opt/OpenMontage_version.txt
       msg_ok "Updated ${APP} to ${RELEASE}"
     else
       msg_ok "No update required. ${APP} is already at ${RELEASE}"
     fi
     exit
   }
   ```

   Changes from current update_script:
   - ADD: empty RELEASE guard (`if [[ -z "${RELEASE}" ]]`) to handle repos without releases (Assumption A1 from research)
   - FIX: `git pull` -> `$STD git pull` (was missing $STD, per UPD-02)
   - ADD: `msg_ok "Pulled ${APP} ${RELEASE}"` after git pull
   - ADD: `msg_info "Reinstalling Python dependencies"` / `msg_ok` wrapper (was missing)
   - ADD: `msg_info "Reinstalling Node.js dependencies"` / `msg_ok` wrapper (was missing)
   - KEEP: `exit` at end (prevents fall-through to build_container)
   - KEEP: .env is never touched (D-07/UPD-04 satisfied by omission)

6. Three mandatory terminal calls:
   ```
   start
   build_container
   description
   ```

7. Success messages:
   ```bash
   msg_ok "Completed successfully!\n"
   echo -e "${CREATING}${GN}${APP} setup has been successfully initialized!${CL}"
   echo -e "${INFO}${YW} Access it using the IP of your LXC Container.${CL}"
   ```

REMOVE entirely (lines 50-71 of current script):
- The `function install_script() { ... }` definition (lines 50-60) — this overrides build.func's install_script() and breaks the entire container creation flow
- The `install_script` call (line 62) — calling the broken override
- The `pct exec "$CTID" -- bash -c "cat > /root/.install_env"` block (lines 67-71) — runs after install script already executed, making it a timing no-op

Per research: NO canonical community-script ct/ script defines a local install_script(). build.func's install_script() handles pve_check, whiptail menu (default/advanced), and delegates to build_container which runs the install/ script inside the LXC.

CT-02 (advanced mode) requires NO code in the ct/ script — build.func's advanced_settings() automatically presents CPU/RAM/disk/hostname/network dialogs when the user selects "Advanced Install" from the whiptail menu.
  </action>
  <verify>
    <automated>shellcheck /Users/johan/dev/Cn/video/ct/openMontage.sh 2>&1; echo "---"; grep -c 'function install_script' /Users/johan/dev/Cn/video/ct/openMontage.sh; echo "---"; grep 'var_disk' /Users/johan/dev/Cn/video/ct/openMontage.sh; echo "---"; grep 'var_gpu' /Users/johan/dev/Cn/video/ct/openMontage.sh; echo "---"; grep -c 'pct exec' /Users/johan/dev/Cn/video/ct/openMontage.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'var_disk.*12' ct/openMontage.sh` matches (D-01, CT-01)
    - `grep 'var_gpu.*no' ct/openMontage.sh` matches (D-02, CT-03)
    - `grep -c 'function install_script' ct/openMontage.sh` returns 0 (CT-04 — no override)
    - `grep -c 'pct exec' ct/openMontage.sh` returns 0 (broken injection removed)
    - `grep '$STD git pull' ct/openMontage.sh` matches (UPD-02)
    - `grep 'msg_info "Reinstalling Python' ct/openMontage.sh` matches (UPD-03)
    - `grep 'msg_info "Reinstalling Node' ct/openMontage.sh` matches (UPD-03)
    - `grep -c '.env' ct/openMontage.sh` returns 0 in update_script body (UPD-04 — .env never touched)
    - Script ends with `start` / `build_container` / `description` sequence
    - `shellcheck ct/openMontage.sh` exits 0 (or only SC1090/SC2034 info-level for sourced build.func)
    - `source <(curl -fsSL` on first line (not `-s` only)
    - `exit` at end of update_script() function
  </acceptance_criteria>
  <done>ct/openMontage.sh matches canonical community-scripts pattern: build.func sourced with -fsSL, var_disk=12, var_gpu=no, no install_script override, no pct exec, update_script with $STD and msg wrappers, start/build_container/description terminal sequence.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up install script dead code from removed API key injection</name>
  <files>install/openMontage-install.sh</files>
  <read_first>
    - install/openMontage-install.sh (current state — 103 lines)
    - ct/openMontage.sh (after Task 1 rewrite — to confirm .install_env no longer written)
  </read_first>
  <action>
Remove the dead `.install_env` sourcing and update the comment in install/openMontage-install.sh.

Change 1 (line 13): Remove the `.install_env` source line entirely:
```
# REMOVE this line:
[[ -f /root/.install_env ]] && source /root/.install_env
```
The ct/ script no longer writes `.install_env` — this file will never exist. Removing dead code.

Change 2 (lines 63-64): Update the comment above the Python API key block:
```
# REPLACE:
  # API keys are collected by ct/openMontage.sh on the host (which has a TTY)
  # and passed in as environment variables FAL_KEY, ELEVENLABS_API_KEY, OPENAI_API_KEY

# WITH:
  # Comment out API key placeholders — users edit .env post-install
```

The Python block itself (lines 65-96) should remain unchanged. It correctly handles the case where env vars are empty (comments out the keys in .env), which is now the only code path since no env vars are injected from the host.

Do NOT change any other lines. Minimal edit per CLAUDE.md.

After editing, run shellcheck on the install script to verify no regressions.
  </action>
  <verify>
    <automated>shellcheck /Users/johan/dev/Cn/video/install/openMontage-install.sh 2>&1; echo "---"; grep -c 'install_env' /Users/johan/dev/Cn/video/install/openMontage-install.sh; echo "---"; grep 'Comment out API' /Users/johan/dev/Cn/video/install/openMontage-install.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'install_env' install/openMontage-install.sh` returns 0 (dead code removed)
    - `grep 'Comment out API key' install/openMontage-install.sh` matches (updated comment)
    - `shellcheck install/openMontage-install.sh` exits 0 (or only SC1090/SC2034 info-level)
    - Lines 100-103 still end with motd_ssh / customize / cleanup_lxc (unchanged)
    - The Python API key block (lines ~65-96) is unchanged — still comments out keys when env vars empty
    - `grep -c 'shellcheck source=/dev/null' install/openMontage-install.sh` returns at least 1 (existing directive preserved)
  </acceptance_criteria>
  <done>install/openMontage-install.sh no longer references .install_env, comment updated to reflect post-install .env editing, ShellCheck passes, all other functionality unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host -> build.func | ct/ script fetches and executes remote code from GitHub |
| Host -> LXC container | build_container creates and runs code inside the container |
| Container -> GitHub API | update_script fetches release info from GitHub |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | build.func source URL | accept | Using official community-scripts URL is required for PR acceptance; HTTPS provides transport integrity. All community-scripts face this same trust model. |
| T-02-02 | Spoofing | GitHub Releases API response | accept | HTTPS to api.github.com; same pattern used by all community-scripts with update mechanisms (gitea, node-red). Low value target (version string only). |
| T-02-03 | Information Disclosure | .env preservation during update | mitigate | update_script() never touches .env file — verified by code omission. No git reset/clean that could delete it. |
| T-02-04 | Denial of Service | Empty RELEASE from GitHub API | mitigate | Added guard: `if [[ -z "${RELEASE}" ]]; then msg_error ...; exit 1; fi` prevents empty-string comparison that would trigger unnecessary updates. |
| T-02-05 | Elevation of Privilege | Container unprivileged default | mitigate | `var_unprivileged="${var_unprivileged:-1}"` defaults to unprivileged container. User can override in advanced mode (acceptable — explicit choice). |
</threat_model>

<verification>
After both tasks complete:
1. `shellcheck ct/openMontage.sh` — zero errors
2. `shellcheck install/openMontage-install.sh` — zero errors
3. `grep -c 'function install_script' ct/openMontage.sh` — returns 0
4. `grep -c 'pct exec' ct/openMontage.sh` — returns 0
5. `grep -c 'install_env' install/openMontage-install.sh` — returns 0
6. `grep 'var_disk.*12' ct/openMontage.sh` — matches
7. `grep 'var_gpu.*no' ct/openMontage.sh` — matches
8. ct/openMontage.sh contains `start` / `build_container` / `description` in sequence
9. update_script() contains `exit` as final statement
</verification>

<success_criteria>
- ct/openMontage.sh matches canonical community-scripts pattern exactly
- All 8 requirements (CT-01 through CT-04, UPD-01 through UPD-04) are satisfied
- No dead code references to removed API key injection mechanism
- Both scripts pass ShellCheck
</success_criteria>

<output>
After completion, create `.planning/phases/02-host-orchestrator-and-update/02-01-SUMMARY.md`
</output>
